'use strict'

/* global describe, it */

const assert = require('assert')

describe('bubbleup-plugin-test-mocha subfolders', function () {
  it('should run mocha tests in subfolders', function (done) {
    assert(typeof done === 'function')
    done()
  })
})
